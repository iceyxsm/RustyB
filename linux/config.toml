# ============================================
# Rust Build Configuration for Linux
# Using standard LLVM backend (more stable)
# ============================================

[build]
# Limit to 2 parallel jobs for 10GB RAM constraint
jobs = 2

[profile.release]
opt-level = 1
debug = false
codegen-units = 256
lto = false
panic = "abort"
strip = true
